\documentclass[tikz,border=2mm]{standalone}
\usetikzlibrary{shapes,arrows}

\begin{document}

\tikzstyle{block} = [draw, rectangle, text width=3cm, text centered, minimum height=1.5cm]
\tikzstyle{sum} = [draw, circle, node distance=1.5cm]
\tikzstyle{line} = [draw, -latex']

\begin{tikzpicture}[auto, node distance=4cm]
    % Nodes
    \node [block] (controller) {Regler};
    \node [block, right of=controller, node distance=3cm] (plant) {Strecke};
    \node [sum, right of=plant] (sum) {$\Sigma$};
    \node [block, right of=sum, node distance=3cm] (sensor) {Sensor};
    \node [block, below of=sum, node distance=2cm] (reference) {Referenzsignal};
    \node [block, below of=sensor, node distance=2cm] (output) {Ausgangssignal};

    % Arrows
    \draw [line] (reference) -- node[pos=0.95] {$+$} (sum);
    \draw [line] (sum) -- node {$U$} (sensor);
    \draw [line] (sensor) -- node {$Y$} (output);
    \draw [line] (controller) -- node {$X$} (sum);
    \draw [line] (plant) -- node {$W$} (sum);

\end{tikzpicture}
\end{document}